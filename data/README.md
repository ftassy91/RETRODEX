# Root Data

This directory is reserved for future shared datasets and manifests at the
repository root.

Current legacy data still lives under:

- `frontend/data/`
- `frontend/datapack/`

No migration is performed in this phase.
