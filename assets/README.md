# Root Assets

This directory is reserved for future shared assets at the repository root.

Current legacy assets still live under:

- `frontend/assets/`
- `frontend/retrodeck_assets/`

No migration is performed in this phase.
