'use strict'

// DATA: compatibility wrapper for local prototype seeding only.
// STATUS: non-canonical, local-only path. Prefer `src/prototype/loadPrototypeData.js`.

module.exports = require('./prototype/loadPrototypeData')
